## Antes de Ejecutar el Comando "php artisan serve"

1. Ejecutar el comando "compose install"
2. Correr las migraciones junto con las seeds (php artisan migrate:fresh --seed)
3. Ejecutar comando "php artisan serve"
